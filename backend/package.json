{
  "name": "joheulttaeda-instagram-oembed-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "test": "node --test server.test.mjs",
    "check": "node --check server.mjs"
  },
  "engines": {
    "node": ">=20.6"
  }
}
